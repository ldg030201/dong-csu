# dong-csu 작업 규칙

두 판이 한 저장소에 있다. **여기에는 양쪽에 다 걸리는 것만 쓴다.**
플랫폼 전용 규칙은 각 폴더에 있다.

| | |
| --- | --- |
| [`mac/CLAUDE.md`](mac/CLAUDE.md) | 빌드 · 렌더 통로 · 서명 · 번들 ID |
| `win/CLAUDE.md` | (윈도우 앱을 만들 때 함께 만든다) |

## 저장소 구조

| | |
| --- | --- |
| `mac/` | macOS 앱 (Swift). 소스·스크립트·맥 전용 문서 |
| `win/` | Windows 앱. 소스·윈도우 전용 문서 |
| `shared/` | 두 판이 나눠 쓰는 데이터. **맥 소스에서 뽑아낸다** |
| `docs/characters/` | 마스코트 문서. 그림이 양쪽 공통이라 뿌리에 둔다 |
| `Formula/` | Homebrew formula. **뿌리에 있어야 한다** — tap이 여기서 찾는다 |

**맥 전용 문서는 `mac/docs/`, 윈도우 전용은 `win/docs/`.** 어느 쪽에도 안 걸리는
것만 뿌리 `docs/`에 둔다. 스크린샷·설치 안내·문제 해결은 화면도 방법도 달라서
공통이 아니다.

## 버전

`주.부.수` 세 자리를 쓰고, **긴급 패치는 네 번째 자리**를 붙인다.

| 자리 | 예 | 언제 |
| --- | --- | --- |
| 주 | `2.0.0` | **크게 뜯어고쳤을 때만.** 아껴서 올린다 |
| 부 | `1.1.0` → `1.12.0` | 기능이 늘거나 화면이 달라질 때 |
| 수 | `1.0.1` | 버그 수정을 모아서 낼 때 |
| 긴급 | `1.0.0.1` | 이미 나간 버전을 급히 고칠 때. 네 번째 자리만 올린다 |

**두 번째 자리는 10을 넘어도 그대로 올린다.** `1.9.0` 다음은 `1.10.0`이고,
자리가 두 자리가 됐다는 이유로 주 버전을 올리지 않는다.

### 판마다 따로 센다

**맥과 윈도우의 번호를 맞추지 않는다.** 고쳐야 할 버그가 서로 달라서, 맞추려 들면
한쪽은 고친 게 없는데 번호만 올라간다. 태그도 갈라 붙인다.

| | 태그 | 지금 |
| --- | --- | --- |
| macOS | `mac-v2.1.0` | 2.1.0 |
| Windows | `win-v1.0.0` | 1.0.0 (첫 배포 준비 중) |

기능은 맥에서 먼저 만들고 윈도우로 옮긴다. 옮겨 온 기능이라도 **윈도우 쪽 번호는
윈도우 사정대로** 올린다.

## 변경 내역 (매번 적는다)

**무언가를 만들거나 고칠 때마다 그 판의 변경 내역 맨 위에 한 줄 추가한다.** 빼먹지 않는다.
아직 안 나간 변경은 날짜가 비어 있는 맨 위 항목에 쌓아 두고, 릴리스할 때 확정한다.

각 판의 설정 창이 이 파일을 그대로 보여주고, 앱이 원격에서 받아보는 JSON도 여기서
뽑는다. **손으로 JSON을 고치지 않는다** — 뽑는 함수를 쓴다. CI가 소스와 다르면
실패시킨다. 어디서 부르는지는 각 판의 `CLAUDE.md`에 있다.

### 문구 규칙

**기능 단위로 한 줄씩, 명사형으로 끝맺는다.** 무엇이 늘었는지·고쳐졌는지가 한눈에
들어와야 한다. 동작을 서술하지 않는다.

| | |
| --- | --- |
| ✅ | `HUD 크기 설정 추가 (작게 / 보통 / 크게 / 매우 크게)` |
| ✅ | `접었다 펴면 CPU·메모리 줄이 사라지던 문제 수정` |
| ✅ | `메뉴바 아이콘을 부엉이로 변경` |
| ❌ | `새 버전이 나오면 HUD 왼쪽 위에 파란 표시가 뜬다` ← 동작 서술 |
| ❌ | `하루에 한 번 새 버전을 확인한다. 버전 탭에서 끌 수 있다` ← 두 문장 |

- 끝맺음은 **추가 / 수정 / 변경 / 개선 / 지원 / 제거** 중에서 고른다
- 덧붙일 게 있으면 괄호로 짧게 (`(하루 1회, 끄기 가능)`)
- 한 줄에 하나씩. 두 가지를 고쳤으면 두 줄로 나눈다
- 내부 구조나 이유는 커밋 메시지·코드 주석에 남기고 여기에는 쓰지 않는다

## 캐릭터

캐릭터마다 [`docs/characters/`](docs/characters/) 아래에 문서가 하나씩 있다.
**새 캐릭터를 만들면 문서도 만들고 목록에 한 줄 더한다.** 그리드·팔레트·기분 설명은
소스가 아니라 거기에 쓴다.

### 두 판이 나눠 쓰는 데이터

부엉이의 **그리드·색·기분별 프레임표**는 [`shared/owl.json`](shared/owl.json) 으로 뽑아
둔다. 윈도우판이 그걸 읽어 같은 그림을 그린다. 손으로 옮겨 적으면 맥에서 자세를
고칠 때마다 어긋나므로 **맥 소스가 유일한 원본**이고 파일은 거기서 뽑는다.

```bash
cd mac && source ./lib.sh && dump_owl
```

**부엉이(그리드·팔레트·기분·걸음걸이·임계값)를 고쳤으면 이걸 다시 돌린다.**
변경 내역과 같은 규칙이고, CI가 소스와 파일이 다르면 실패시킨다.

프레임마다 **합성이 끝난 15×13 그리드**가 같이 들어간다. 레이어 겹치기는 알고리즘이라
윈도우가 새로 구현해야 하는데, 틀리면 이 그리드와 비교해서 바로 잡을 수 있다 —
그림을 그려보지 않고 텍스트만으로 검증된다.

새 레이어를 만들면 `OwlMark.layers` 목록에도 한 줄 더한다. 빠뜨리면 그 파츠만
윈도우판에 안 넘어간다.

**윈도우가 부엉이를 고치고 싶으면 맥 소스를 고친다.** `shared/owl.json`을 직접
편집하면 다음 `dump_owl`에 지워지고, CI가 먼저 잡는다.

## 이름

화면에 보이는 이름은 **DongCSU**, 명령과 Homebrew tap은 `dong-csu`다.
`CSU`는 Claude Status UI다.

`dong-mcu` → `dong-csu` 개명(맥 v2.0.0) 때 번들 ID까지 바꿨다. `m`이 macOS를 뜻했는데
윈도우판을 만들기로 하면서 틀린 글자가 됐고, 사용자가 적을 때 치우는 게 쌌다.

## 커밋

**제목 한 줄만 쓴다. 본문도 푸터도 붙이지 않는다.**

이모지 하나 + 한국어 한 줄이 전부다(`✨ 기능`, `🐛 수정`, `🎨 모양`, `📝 문서`,
`🔧 설정`, `⚡ 성능`, `🔖 버전`, `📦 formula`).

- 여러 줄로 설명하고 싶어지면 커밋을 쪼갠다
- `Co-Authored-By` 같은 푸터도 넣지 않는다
- 왜 그렇게 했는지 남길 값어치가 있으면 코드 주석이나 이 문서에 적는다
- **어느 판인지 제목에 굳이 적지 않는다** — 바뀐 파일 경로가 이미 말해 준다
