# dong-csu 작업 규칙

두 판이 한 저장소에 있다. **여기에는 양쪽에 다 걸리는 것만 쓴다.**
플랫폼 전용 규칙은 각 폴더에 있다.

| | |
| --- | --- |
| [`mac/CLAUDE.md`](mac/CLAUDE.md) | 빌드 · 렌더 통로 · 서명 · 번들 ID |
| [`win/CLAUDE.md`](win/CLAUDE.md) | 프로젝트 구성 · 진단 통로 · 배포 · 그릴 때 걸리는 것 |

## 저장소 구조

| | |
| --- | --- |
| `mac/` | macOS 앱 (Swift). 소스·스크립트·맥 전용 문서 |
| `win/` | Windows 앱. 소스·윈도우 전용 문서 |
| `shared/` | 두 판이 나눠 쓰는 데이터. **맥 소스에서 뽑아낸다** |
| `docs/characters/` | 마스코트 문서. 그림이 양쪽 공통이라 뿌리에 둔다 |
| `Formula/` | Homebrew formula. **뿌리에 있어야 한다** — tap이 여기서 찾는다 |

**맥 전용 문서는 `mac/docs/`, 윈도우 전용은 `win/docs/`.** 어느 쪽에도 안 걸리는
것만 뿌리 `docs/`에 둔다. 스크린샷·설치 안내·문제 해결은 화면도 방법도 달라서
공통이 아니다.

개발 방식(테스트판과 정식판을 나눠 놓고 쓰는 것, 화면 없이 확인하는 통로)은
[`docs/development.md`](docs/development.md) 에 정리돼 있다.

## 버전

`주.부.수` 세 자리를 쓰고, **긴급 패치는 네 번째 자리**를 붙인다.

| 자리 | 예 | 언제 |
| --- | --- | --- |
| 첫 번째 | `2.0.0` | **크게 뜯어고쳤을 때만.** 아껴서 올린다 |
| 두 번째 | `1.1.0` → `1.12.0` | **기능 추가 · 기능 변경** |
| 세 번째 | `1.0.1` | **오류 수정 · 소소한 설정이나 기능 변경** |
| 네 번째 | `1.0.0.1` | **긴급** 수정. 이미 나간 버전을 급히 고칠 때만 |

**두 번째 자리는 10을 넘어도 그대로 올린다.** `1.9.0` 다음은 `1.10.0`이고,
자리가 두 자리가 됐다는 이유로 첫 자리를 올리지 않는다.

### 두 번째냐 세 번째냐

헷갈리는 건 여기뿐이다. **없던 것이 생겨서 쓰는 방법이 달라지면 두 번째,
있던 것을 손보거나 설정이 하나 늘어난 정도면 세 번째.**

| | 자리 | 왜 |
| --- | --- | --- |
| `펫 모드 추가` | 두 번째 | 없던 보기가 생겼다 |
| `HUD 크기 설정 추가 (4단계)` | 두 번째 | 화면이 달라진다 |
| `혼자 돌아다니기 추가` | 두 번째 | 없던 동작이다 |
| `로그인할 때 자동 시작 설정 추가` | **세 번째** | 설정 하나가 늘었을 뿐, 쓰던 방법은 그대로다 |
| `커서 피하기 기본값을 켜짐으로 변경` | **세 번째** | 있던 것의 기본값만 바뀌었다 |
| `우클릭 메뉴에서 겹치는 항목 제거` | **세 번째** | 설정 창에 다 있어서 없어진 기능이 없다 |
| `접었다 펴면 CPU 줄이 사라지던 문제 수정` | **세 번째** | 오류 수정 |

**애매하면 세 번째로 간다.** 두 번째 자리를 아껴 두면 나중에 "이 버전에서 뭐가
늘었더라"를 번호만 보고 짚을 수 있다.

### 네 번째 자리가 세 번째보다 우선한다

내용이 아무리 소소해도 **방금 나간 버전을 급히 고치는 것이면 네 번째 자리다.**
`1.0.0` 을 낸 직후에 문구 하나를 고쳤다면 `1.0.1` 이 아니라 `1.0.0.1` 이다.
자리를 고를 때 *무엇을 고쳤나* 보다 *왜 지금 내나* 를 먼저 본다.

### 릴리스는 혼자 올리지 않는다

**버전 자리를 정하거나 태그를 붙이기 전에 반드시 물어본다.** 코드가 다 되고 검사를
전부 통과해도 릴리스는 별개다. "다 됐다, 어느 자리로 올릴까?" 까지가 여기서 할 일이다.

되돌리기가 비싸기 때문이다 — 태그·GitHub 릴리스·자체 업데이트 피드가 한꺼번에 나가고,
**누가 하나라도 받아 가면 그 번호는 영영 고정된다.**

### 판마다 따로 센다

**맥과 윈도우의 번호를 맞추지 않는다.** 고쳐야 할 버그가 서로 달라서, 맞추려 들면
한쪽은 고친 게 없는데 번호만 올라간다. 태그도 갈라 붙인다.

| | 태그 |
| --- | --- |
| macOS | `mac-v2.3.0` |
| Windows | `win-v1.2.0` |

**지금 나가 있는 번호는 여기 적지 않는다.** 손으로 적어 두면 반드시 낡는다 —
릴리스가 올려 주는 뿌리 [`README.md`](README.md) 의 "지금 버전" 줄을 보거나
`git tag` 를 본다.

**윈도우는 네 번째 자리를 쓰지 못한다.** 설치본이 NuGet 패키지 형식이라 버전이
**SemVer2 세 자리**여야 하고, `1.0.0.1` 은 패키징 단계에서 거부당한다. 그래서
윈도우의 긴급 수정도 세 번째 자리로 올린다. 자세한 건 [`win/CLAUDE.md`](win/CLAUDE.md).

기능은 맥에서 먼저 만들고 윈도우로 옮긴다. 옮겨 온 기능이라도 **윈도우 쪽 번호는
윈도우 사정대로** 올린다.

## 변경 내역 (매번 적는다)

**무언가를 만들거나 고칠 때마다 그 판의 변경 내역 맨 위에 한 줄 추가한다.** 빼먹지 않는다.
아직 안 나간 변경은 날짜가 비어 있는 맨 위 항목에 쌓아 두고, 릴리스할 때 확정한다.

각 판의 설정 창이 이 파일을 그대로 보여주고, 앱이 원격에서 받아보는 JSON도 여기서
뽑는다. **손으로 JSON을 고치지 않는다** — 뽑는 함수를 쓴다. CI가 소스와 다르면
실패시킨다. 어디서 부르는지는 각 판의 `CLAUDE.md`에 있다.

### 문구 규칙

**기능 단위로 한 줄씩, 명사형으로 끝맺는다.** 무엇이 늘었는지·고쳐졌는지가 한눈에
들어와야 한다. 동작을 서술하지 않는다.

| | |
| --- | --- |
| ✅ | `HUD 크기 설정 추가 (작게 / 보통 / 크게 / 매우 크게)` |
| ✅ | `접었다 펴면 CPU·메모리 줄이 사라지던 문제 수정` |
| ✅ | `메뉴바 아이콘을 부엉이로 변경` |
| ❌ | `새 버전이 나오면 HUD 왼쪽 위에 파란 표시가 뜬다` ← 동작 서술 |
| ❌ | `하루에 한 번 새 버전을 확인한다. 버전 탭에서 끌 수 있다` ← 두 문장 |

- 끝맺음은 **추가 / 수정 / 변경 / 개선 / 지원 / 제거** 중에서 고른다
- 덧붙일 게 있으면 괄호로 짧게 (`(하루 1회, 끄기 가능)`)
- 한 줄에 하나씩. 두 가지를 고쳤으면 두 줄로 나눈다
- 내부 구조나 이유는 커밋 메시지·코드 주석에 남기고 여기에는 쓰지 않는다

### 묶음과 갈래 (맥 2.3.0부터)

한 버전에 스무 줄이 쌓이면 평평한 목록으로는 뭐가 달라졌는지 안 잡힌다. **기능 단위로
묶고**(`ChangelogGroup`) 묶음 이름은 화면·메뉴 이름을 그대로 쓴다 — `측정`, `펫 모드`.
항목마다 **갈래**(`신규 / 개선 / 변경 / 오류 / 제거`)를 단다.

**설정 탭 이야기면 `tab` 에 그 탭을 적는다.** 제목 앞에 그 탭에 붙어 있는 아이콘이
그대로 나와서, 어느 메뉴 이야기인지 사이드바와 눈으로 맞춰 볼 수 있다. 아이콘 이름을
따로 적지 않는 이유가 이거다 — 탭 아이콘을 바꾸면 변경 내역도 같이 바뀌어야 한다.
**탭에 없는 것**(마스코트·HUD·설치)은 비워 두면 공통 아이콘 하나로 묶인다.

**새로 생긴 기능 묶음(`isNew`)에는 갈래를 붙이지 않는다.** 무엇이 있는지만 적는다.

없던 기능을 만들면서 우리가 고친 오류·바꾼 동작·지운 설정은 **나간 적이 없다.**
그걸 적으면 사용자는 겪은 적도 없는 버그와, 써 본 적도 없는 기능이 없어졌다는 말을
읽는다. 그건 우리 개발 과정이지 사용자의 변경 내역이 아니다.

| | |
| --- | --- |
| ✅ | `측정` **신규** → `측정 기록 목록 (중지하면 남고, 누르면 그때 값을 펼쳐 봄)` |
| ❌ | `측정` **신규** → `[오류] 다시 시작하면 지난 기록이 사라지던 문제 수정` ← 나간 적 없다 |
| ❌ | `측정` **신규** → `[제거] 측정 초기화 제거` ← 있던 적이 없다 |

갈래는 **이전 판에 나가 있던 것**을 손봤을 때만 쓴다. 그래서 신규 묶음의 줄은
끝맺음도 `추가`를 붙이지 않는다 — 묶음 전체가 새것이라 줄마다 반복할 말이 아니다.

**이미 나간 버전은 뒤늦게 나누지 않는다.** 사용자가 그때 본 것과 달라진다.
2.2.0 이하는 평평한 목록 그대로 둔다.

옛 앱도 같은 JSON을 받아본다. 평평한 `notes` 는 `groups` 에서 뽑아 내므로 **손으로
적지 않는다** — 두 곳에 적으면 반드시 어긋난다.

## 캐릭터

캐릭터마다 [`docs/characters/`](docs/characters/) 아래에 문서가 하나씩 있다.
**새 캐릭터를 만들면 문서도 만들고 목록에 한 줄 더한다.** 그리드·팔레트·기분 설명은
소스가 아니라 거기에 쓴다.

### 두 판이 나눠 쓰는 데이터

부엉이의 **그리드·색·기분별 프레임표**는 [`shared/owl.json`](shared/owl.json) 으로 뽑아
둔다. 윈도우판이 그걸 읽어 같은 그림을 그린다. 손으로 옮겨 적으면 맥에서 자세를
고칠 때마다 어긋나므로 **맥 소스가 유일한 원본**이고 파일은 거기서 뽑는다.

```bash
cd mac && source ./lib.sh && dump_owl
```

**부엉이(그리드·팔레트·기분·걸음걸이·임계값)를 고쳤으면 이걸 다시 돌린다.**
변경 내역과 같은 규칙이고, CI가 소스와 파일이 다르면 실패시킨다.

프레임마다 **합성이 끝난 15×13 그리드**가 같이 들어간다. 레이어 겹치기는 알고리즘이라
윈도우가 새로 구현해야 하는데, 틀리면 이 그리드와 비교해서 바로 잡을 수 있다 —
그림을 그려보지 않고 텍스트만으로 검증된다.

새 레이어를 만들면 `OwlMark.layers` 목록에도 한 줄 더한다. 빠뜨리면 그 파츠만
윈도우판에 안 넘어간다.

**윈도우가 부엉이를 고치고 싶으면 맥 소스를 고친다.** `shared/owl.json`을 직접
편집하면 다음 `dump_owl`에 지워지고, CI가 먼저 잡는다.

## 이름

화면에 보이는 이름은 **DongCSU**, 명령과 Homebrew tap은 `dong-csu`다.
`CSU`는 Claude Status UI다.

`dong-mcu` → `dong-csu` 개명(맥 v2.0.0) 때 번들 ID까지 바꿨다. `m`이 macOS를 뜻했는데
윈도우판을 만들기로 하면서 틀린 글자가 됐고, 사용자가 적을 때 치우는 게 쌌다.

## 커밋

**제목 한 줄만 쓴다. 본문도 푸터도 붙이지 않는다.**

`[판] 이모지 하나 한국어 한 줄` 이 전부다.

```
[Mac] ✨ 펫 모드에 설정·새로고침 버튼 추가
[Win] 🐛 배율이 100%가 아닌 화면에서 위치가 초기화되던 문제 수정
📝 커밋 앞머리 규칙 추가
```

### 앞머리

**어느 판을 고쳤는지 제목 맨 앞에 붙인다.** 두 판이 한 저장소에 있어서 기록이 섞이는데,
경로를 펴 보지 않아도 `git log` 한 줄로 갈라 볼 수 있어야 한다.

| | 언제 |
| --- | --- |
| `[Mac]` | `mac/` 안만 고쳤을 때 |
| `[Win]` | `win/` 안만 고쳤을 때 |
| **안 붙인다** | 양쪽에 걸리거나 어느 쪽도 아닐 때 — `shared/`, 뿌리 문서, `.github/`, `Formula/` |

**두 판을 같이 고쳤으면 커밋을 쪼갠다.** 앞머리를 못 고르겠다는 건 한 커밋에 두 가지가
들어갔다는 신호다. `Formula/`는 맥 것이지만 뿌리에 있고 릴리스 때만 바뀌므로 안 붙인다.

### 나머지

이모지는 `✨ 기능`, `🐛 수정`, `🎨 모양`, `📝 문서`, `🔧 설정`, `⚡ 성능`, `🔖 버전`,
`📦 formula` 중에서 고른다.

- 여러 줄로 설명하고 싶어지면 커밋을 쪼갠다
- `Co-Authored-By` 같은 푸터도 넣지 않는다
- 왜 그렇게 했는지 남길 값어치가 있으면 코드 주석이나 이 문서에 적는다
